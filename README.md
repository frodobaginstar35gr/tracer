# Tracer

A Chrome extension that intercepts outbound browser requests, identifies trackers, and uses the Claude API to classify them by category (analytics, advertising, fingerprinting, etc.). Users can generate GDPR/CCPA data deletion request letters with one click.

**Status:** Paused / on hold.
